{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "create-secret output",
    "$id": "http://schema.nethserver.org/grantora/create-secret-output.json",
    "type": "object",
    "required": ["action", "secret"],
    "properties": {
        "action": {"type": "string", "enum": ["created", "reused"]},
        "secret": {"type": "object"}
    }
}
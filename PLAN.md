# PLAN.md

Roadmap for turning the freshly cloned `ns8-kickstart` repository into a production-capable NethServer 8 module that installs, configures, operates and upgrades the standalone Grantora Agent Capability Gateway.

## Goal

`ns8-grantora` packages upstream Grantora for NS8. It must not fork Grantora application logic.

Runtime target:

```text
Agent / automation client
  -> NS8 Traefik public route
  -> 127.0.0.1:<runtime_port>
  -> Grantora Podman pod
  -> APISIX runtime port 9080
  -> grantora-api internal port 8080
  -> PostgreSQL internal port 5432
  -> adapter -> upstream business application
```

Upstream Grantora runtime components:

```text
grantora-api     FastAPI service: runtime API, admin API, invocation, audit, usage, APISIX reconciliation
postgres         source of truth for Grantora dynamic state
apisix           HTTP data-plane and runtime gateway
apisix-etcd      APISIX runtime configuration backend
```

NS8 module responsibilities:

- create one `grantora` Podman pod per module instance;
- publish only the APISIX runtime port from the pod to host loopback;
- launch each pod container with its own systemd user unit;
- generate static environment configuration from NS8 actions;
- store generated secrets in NS8 state;
- expose only APISIX runtime endpoints publicly through Traefik;
- keep Grantora Admin API, APISIX Admin API, PostgreSQL and etcd private inside the pod;
- provide admin UI actions for setup, status, user-provider selection and common Grantora administration;
- bind to the selected NS8 user domain with `accountconsumer` authorization;
- sync selected LDAP users into Grantora users;
- back up and restore PostgreSQL plus environment-managed secret material;
- run APISIX reconciliation after configure, restore and upgrade;
- provide lifecycle, smoke, security and Robot tests.

Upstream Grantora responsibilities remain:

- runtime/admin API behavior;
- capability authorization;
- RBAC and bindings;
- adapter execution;
- secret encryption and resolution;
- audit and usage records;
- generated APISIX route desired state;
- filtered OpenAPI and MCP-compatible tool descriptions.

## Non-negotiable module rules

- Do not expose raw upstream application APIs.
- Do not expose Grantora Admin API publicly unless a future explicit private/operator route contract is added.
- Do not expose APISIX Admin API, PostgreSQL or etcd on public interfaces.
- Do not publish individual container ports directly; port mapping belongs to `grantora-pod.service` only.
- Do not add a default host-mapped admin port. Admin actions must call Grantora from inside the pod.
- Do not store upstream application secrets in agent configuration.
- Do not log tokens, passwords, authorization headers, encrypted secret values or decrypted secrets.
- Do not make upstream Grantora depend on NS8 paths, commands or Redis.
- PostgreSQL is Grantora source of truth; APISIX etcd is generated runtime state.
- Environment files are generated by the module; hand-edited config files are not a runtime contract.
- User provider selection is NS8-owned: the module lists available account domains, the admin selects one, and the module binds to it.

## Pod and systemd topology

Grantora must run as one rootless Podman pod controlled by systemd user units.

```text
grantora.service                 aggregate service, starts/stops the whole module runtime
grantora-pod.service             creates/replaces the pod and owns all host port mappings
grantora-postgres.service        helper container in the pod
grantora-apisix-etcd.service     helper container in the pod
grantora-api.service             upstream Grantora API container in the pod
grantora-apisix.service          APISIX data-plane container in the pod
grantora-user-sync.timer         periodic LDAP -> Grantora user sync
grantora-user-sync.service       one-shot sync service
```

Systemd ordering:

```text
grantora.service
  Wants=grantora-pod.service
  Wants=grantora-postgres.service
  Wants=grantora-apisix-etcd.service
  Wants=grantora-api.service
  Wants=grantora-apisix.service

grantora-postgres.service
  Requires=grantora-pod.service
  After=grantora-pod.service

grantora-apisix-etcd.service
  Requires=grantora-pod.service
  After=grantora-pod.service

grantora-api.service
  Requires=grantora-pod.service grantora-postgres.service grantora-apisix-etcd.service
  After=grantora-pod.service grantora-postgres.service grantora-apisix-etcd.service

grantora-apisix.service
  Requires=grantora-pod.service grantora-api.service grantora-apisix-etcd.service
  After=grantora-pod.service grantora-api.service grantora-apisix-etcd.service
```

Pod rules:

```text
pod name: grantora
containers join with: --pod=grantora
published host ports: only 127.0.0.1:<runtime_port>:9080 by default
container-to-container URLs: use 127.0.0.1 inside the pod network namespace
```

Internal URLs inside the pod:

```text
DATABASE_URL=postgresql+psycopg://grantora:${POSTGRES_PASSWORD}@127.0.0.1:5432/grantora
APISIX_ADMIN_URL=http://127.0.0.1:9180
APISIX_RUNTIME_UPSTREAM_NODE=127.0.0.1:8080
APISIX_ETCD_ENDPOINT=http://127.0.0.1:2379
GRANTORA_API_INTERNAL_URL=http://127.0.0.1:8080
GRANTORA_RUNTIME_INTERNAL_URL=http://127.0.0.1:9080
```

Admin actions must not require a host admin port. Use one of these mechanisms:

```text
preferred: podman exec grantora-api python -m <safe module/admin helper>
accepted:  podman exec grantora-api python -c '<small urllib/httpx call to http://127.0.0.1:8080>'
accepted:  short-lived helper container attached to --pod=grantora when a stable helper image exists
rejected:  publishing grantora-api:8080 on the host by default
```

## Target module structure

Follow `STRUCTURE.md` and keep the systemd layout explicit.

```text
README.md
AGENTS.md
PLAN.md
STRUCTURE.md
build-images.sh
imageroot/
  actions/
    get-defaults/
    get-configuration/
    configure-module/
    get-status/
    sync-users/
    bootstrap-workspace/
    rotate-module-secrets/
    run-smoke/
    backup-module/
    restore-module/
    upgrade-module/
    destroy-module/
  events/
    user-domain-changed/
    module-domain-changed/
  systemd/user/
    grantora.service
    grantora-pod.service
    grantora-postgres.service
    grantora-apisix-etcd.service
    grantora-api.service
    grantora-apisix.service
    grantora-user-sync.timer
    grantora-user-sync.service
  bin/
    grantora-admin
    grantora-env
    grantora-lifecycle
    grantora-pg-dump
    grantora-pg-restore
    grantora-pod-exec
    grantora-smoke
    module-dump-state
    module-cleanup-state
  etc/
    state-include.conf
    state-exclude.conf
ui/
tests/
.github/
```

State files:

```text
state/environment              non-secret module settings via agent.set_env()
state/secrets.env              module-generated secrets, mode 0600
state/grantora.env             generated grantora-api env file
state/postgres.env             generated PostgreSQL env file
state/apisix.env               generated APISIX env file
state/apisix-config.yaml       generated APISIX config for pod-local etcd
state/runtime.env              generated ports, URLs, pod/container names
state/user-domain.json         selected provider metadata and sync cursor
state/bootstrap.json           Grantora object ids created by bootstrap actions
state/upgrade.json             latest safe upgrade record, no secrets
state/upgrades/*.json          historical safe upgrade records, no secrets
state/secret-rotation.json     latest safe module secret rotation record, no secrets
state/agent-tokens/*.env       one-time agent tokens only when explicitly generated by admin action
state/backups/                 temporary local dumps used by backup/restore actions
state/postgres-data/           PostgreSQL volume bind mount
state/apisix-etcd-data/        APISIX etcd volume bind mount, generated state only
```

Secrets to generate and preserve:

```text
POSTGRES_PASSWORD
APISIX_ADMIN_KEY
SECRET_ENCRYPTION_KEY
GRANTORA_AGENT_TOKEN_PEPPER
ADMIN_BOOTSTRAP_TOKEN                 module-only plaintext operator token for actions
GRANTORA_ADMIN_BOOTSTRAP_TOKEN_HASH   passed to grantora-api
```

Non-secret settings to persist:

```text
GRANTORA_IMAGE
GRANTORA_VERSION
GRANTORA_PUBLIC_BASE_URL
GRANTORA_LOG_LEVEL
METRICS_ENABLED
AUDIT_RETENTION_DAYS
USAGE_RETENTION_DAYS
APISIX_SYNC_ENABLED
APISIX_SYNC_INTERVAL_SECONDS
APISIX_FAIL_CLOSED
APISIX_RATE_LIMIT_COUNT
APISIX_RATE_LIMIT_TIME_WINDOW
UPSTREAM_TIMEOUT_SECONDS
UPSTREAM_CONNECT_TIMEOUT_SECONDS
UPSTREAM_TLS_VERIFY
UPSTREAM_MAX_RESPONSE_BYTES
UPSTREAM_READ_RETRY_ATTEMPTS
LDAP_USER_DOMAIN
TCP_PORT
```

## Runtime exposure model

Default exposure:

```text
host loopback: 127.0.0.1:<TCP_PORT>
pod port:      APISIX 9080
route:         Traefik https://<host>/ -> http://127.0.0.1:<TCP_PORT>
```

Only `<TCP_PORT>` receives a Traefik route.

Never publish these services to the host:

```text
Grantora API 8080
PostgreSQL 5432
APISIX Admin API 9180
APISIX etcd 2379
```

Private/operator surfaces are reachable only from inside the pod through `grantora-admin`/`grantora-pod-exec` helpers:

```text
http://127.0.0.1:8080/v1/admin/*
http://127.0.0.1:8080/healthz
http://127.0.0.1:8080/readyz
http://127.0.0.1:8080/metrics when enabled
http://127.0.0.1:9180/* APISIX Admin API
```

If a future feature adds operator route exposure, it must be a separate explicit milestone with trusted-proxy/IP-allowlist/mTLS policy. It must not be added as an incidental configure option.

## Configure-module input contract draft

```json
{
  "host": "grantora.example.org",
  "user_domain": "ad.example.org",
  "grantora_image": "ghcr.io/grantora/grantora-api",
  "grantora_version": "0.1.0",
  "metrics_enabled": true,
  "log_level": "INFO",
  "runtime_rate_limit_count": 1000,
  "runtime_rate_limit_time_window": 60,
  "audit_retention_days": 365,
  "usage_retention_days": 365,
  "upstream_tls_verify": true,
  "upstream_timeout_seconds": 30,
  "sync_users_enabled": true,
  "sync_users_interval_minutes": 60
}
```

`get-configuration` must return current values plus the list of available NS8 user domains so the admin UI can render a provider selector.

## User provider integration design

- Add `org.nethserver.authorizations=cluster:accountconsumer` to the module image labels.
- Discover available account domains with `agent.ldapproxy.Ldapproxy().get_domains_list()`.
- In `get-configuration`, return the available domains with at least: domain name, schema, provider module when available, current selection flag, and whether the domain is reachable.
- In `configure-module`, require `user_domain` unless the module is explicitly configured in standalone/no-user-sync mode.
- Bind the selected domain with `agent.bind_user_domains([user_domain])`.
- Store the selected domain in `state/environment` as `LDAP_USER_DOMAIN` and in `state/user-domain.json` with sync metadata.
- Handle `user-domain-changed` and `module-domain-changed` events by scheduling or running `sync-users`.
- `sync-users` reads users through `agent.ldapclient` using filters from `agent.ldapproxy` so hidden users are not imported.
- Sync users into Grantora through Admin APIs using the LDAP username as Grantora `external_id`.
- Never pass LDAP passwords to Grantora and never store raw user passwords.

Initial sync behavior:

```text
LDAP user exists and Grantora user missing      -> create active Grantora user
LDAP user exists and Grantora user disabled     -> reactivate only if configured to do so
LDAP user disappeared/hidden/disabled           -> disable Grantora user if managed by selected domain
LDAP display name changed                       -> update when upstream supports metadata update
```

Until upstream bulk/idempotent sync exists, implement the action with paginated `GET /v1/admin/users`, `POST /v1/admin/users` and `PATCH /v1/admin/users/{id}` where possible.

## Milestone 0 — Repository initialization

Objective: replace kickstart identity with `ns8-grantora` identity and make the repository ready for agentic work.

Tasks:

- [x] Replace kickstart README with Grantora module README.
- [x] Add `AGENTS.md` with NS8 module rules and Grantora-specific boundaries.
- [x] Keep `STRUCTURE.md` aligned with the actual module layout.
- [x] Keep this `PLAN.md` aligned with upstream Grantora runtime structure.
- [x] Rename kickstart systemd units, tests and workflow references to `grantora`.
- [x] Set correct image labels in `build-images.sh`:
  - [x] module name and description;
  - [x] required TCP port demand: `1` for APISIX runtime route;
  - [x] `org.nethserver.authorizations=cluster:accountconsumer`.
- [x] Remove unused smarthost example logic from kickstart unless it is explicitly needed later.
- [x] Add shell/Python lint configuration for action scripts.

Acceptance:

- [x] Repository no longer exposes kickstart names in user-facing files.
- [x] `build-images.sh` builds a module image named for `ns8-grantora`.
- [x] CI validates markdown, shell scripts and action schemas.

Upstream Grantora work:

- [ ] None required.

## Milestone 1 — Pod-based runtime packaging skeleton

Objective: start the full Grantora runtime stack as one NS8 rootless Podman pod, with separate systemd units for each helper container.

Tasks:

- [x] Create `grantora-pod.service`:
  - [x] creates/replaces pod `grantora`;
  - [x] publishes only `127.0.0.1:${TCP_PORT}:9080`;
  - [x] does not publish Grantora API, PostgreSQL, APISIX Admin API or etcd;
  - [x] owns pod-level network lifecycle.
- [x] Create `grantora-postgres.service`:
  - [x] runs `postgres:16-alpine` or approved image;
  - [x] joins `--pod=grantora`;
  - [x] uses `state/postgres.env`;
  - [x] bind-mounts `state/postgres-data/`;
  - [x] exposes only pod-local `127.0.0.1:5432`.
- [x] Create `grantora-apisix-etcd.service`:
  - [x] joins `--pod=grantora`;
  - [x] bind-mounts `state/apisix-etcd-data/`;
  - [x] listens only pod-local on `127.0.0.1:2379` or pod-local equivalent.
- [x] Create `grantora-api.service`:
  - [x] runs upstream versioned Grantora API image;
  - [x] joins `--pod=grantora`;
  - [x] uses `state/grantora.env` plus `state/secrets.env`;
  - [x] sets `DATABASE_URL` to pod-local PostgreSQL;
  - [x] sets `APISIX_ADMIN_URL` to pod-local APISIX Admin API;
  - [x] sets `APISIX_RUNTIME_UPSTREAM_NODE=127.0.0.1:8080`;
  - [x] does not publish ports.
- [x] Create `grantora-apisix.service`:
  - [x] joins `--pod=grantora`;
  - [x] uses `state/apisix.env`;
  - [x] renders APISIX config for pod-local etcd;
  - [x] exposes runtime `9080` only through the pod mapping;
  - [x] keeps Admin API `9180` pod-local only.
- [x] Create aggregate `grantora.service` that starts/stops the pod and all container units.
- [x] Generate env files from state and secrets.
- [x] Add service health checks using `/healthz`, `/readyz`, PostgreSQL readiness and APISIX runtime reachability.
- [x] Ensure helpers can be restarted independently without recreating the pod unless the pod networking changed.

Acceptance:

- [ ] A fresh module instance starts one pod and four containers.
- [ ] `systemctl --user status grantora.service` shows the aggregate status.
- [ ] `systemctl --user status grantora-postgres.service` and other helper services are inspectable independently.
- [ ] `grantora-api /healthz` returns the expected version from inside the pod.
- [ ] `grantora-api /readyz` succeeds after PostgreSQL is ready.
- [ ] APISIX reaches `grantora-api` through pod-local `127.0.0.1:8080`.
- [ ] No public Traefik route exists yet.
- [ ] No default host-mapped admin/API/database/etcd ports exist.

Upstream Grantora work:

- [ ] Verify the published Grantora image namespace and immutable tag strategy.
- [ ] Keep `deploy/compose.production.yml` and `docs/ns8-packaging.md` aligned with the module runtime shape, while allowing NS8 to replace Compose networks with a Podman pod.

## Milestone 2 — First configure action and public runtime route

Objective: configure host, generated secrets, pod env files, Traefik route and APISIX reconciliation.

Tasks:

- [x] Implement `get-defaults` with secure defaults and generated runtime values.
- [x] Implement `configure-module` JSON schema.
- [x] Validate hostnames, retention values, rate limits, timeout values and image tags.
- [x] Generate missing secrets only once; preserve them across reconfigure.
- [x] Compute `GRANTORA_ADMIN_BOOTSTRAP_TOKEN_HASH` from the module-only `ADMIN_BOOTSTRAP_TOKEN` and `GRANTORA_AGENT_TOKEN_PEPPER`.
- [x] Render `grantora.env`, `postgres.env`, `apisix.env`, `apisix-config.yaml` and `runtime.env`.
- [x] Start/restart `grantora.service`.
- [x] Configure an NS8 Traefik route from `https://<host>/` to `http://127.0.0.1:${TCP_PORT}`.
- [x] Set `GRANTORA_PUBLIC_BASE_URL=https://<host>`.
- [x] Call `POST /v1/admin/apisix/sync` through `grantora-admin`, not a host-mapped admin port.
- [x] Implement `get-status` with:
  - [x] pod status;
  - [x] each container unit status;
  - [x] `/healthz` result;
  - [x] `/readyz` result;
  - [x] APISIX sync status;
  - [x] configured public URL;
  - [x] selected user domain;
  - [x] running upstream version.

Acceptance:

- [ ] Configure from CLI creates a working public runtime URL.
- [ ] `GET https://<host>/v1/openapi.json` works through APISIX with runtime-only OpenAPI.
- [ ] Admin API is not exposed by Traefik.
- [ ] APISIX Admin API is not externally reachable.
- [ ] PostgreSQL and etcd are not externally reachable.
- [ ] Re-running `configure-module` is idempotent and does not rotate secrets unless requested.

Upstream Grantora work:

- [ ] Add or verify stable startup APISIX sync behavior with safe failure semantics.
- [ ] Add release-image smoke evidence for the image tag selected by the module.

## Milestone 3 — NS8 user provider selection and LDAP user sync

Objective: allow the Grantora admin in NS8 UI to select one installed NS8 user provider and sync users into Grantora.

Tasks:

- [x] Implement domain discovery helper in `imageroot/bin/` or an action fragment using `agent.ldapproxy`.
- [x] Return available user domains from `get-configuration`.
- [x] Add `user_domain` field to `configure-module`.
- [x] Bind the module to the selected domain with `agent.bind_user_domains([domain])`.
- [x] Persist selected domain and sync settings.
- [x] Implement `sync-users` action:
  - [x] load selected domain connection parameters;
  - [x] list visible users with `agent.ldapclient`;
  - [x] create missing Grantora users through Admin API;
  - [x] disable missing/hidden users when the module owns them;
  - [x] record sync summary in `state/user-domain.json`.
- [x] Add `grantora-user-sync.timer` and `grantora-user-sync.service`.
- [x] Add `events/user-domain-changed/` handler.
- [x] Add `events/module-domain-changed/` handler.
- [x] Add admin UI button: `Sync users now`.
- [x] Add UI state: last sync, imported count, disabled count, error count.

Acceptance:

- [ ] Admin UI shows available NS8 user providers.
- [ ] Admin can select one provider during module configuration.
- [ ] NS8 shows the module/domain binding relation.
- [ ] Grantora users are created with LDAP username as `external_id`.
- [ ] Hidden users are not imported.
- [ ] Removing or disabling a user in the provider disables the managed Grantora user after sync.
- [ ] No LDAP password is stored or sent to Grantora.

Upstream Grantora work:

- [ ] Add a production-grade idempotent user sync API, for example `POST /v1/admin/users/sync`, with:
  - [ ] workspace id;
  - [ ] source id, e.g. `ns8:<domain>`;
  - [ ] users array with `external_id`, `display_name`, optional email and group ids;
  - [ ] `disable_missing` flag;
  - [ ] safe audit summary;
  - [ ] no secret material in responses.
- [ ] Add mutable user metadata support beyond lifecycle status.
- [ ] Add optional user group metadata if Grantora will later map NS8 groups to roles/bindings.
- [ ] Add tests proving user sync cannot cross workspace boundaries.

## Milestone 4 — Grantora bootstrap and module admin helpers

Objective: provide actions that create the first usable Grantora workspace and common objects without requiring manual curl commands.

Tasks:

- [x] Implement `grantora-admin` helper that executes Admin API calls inside the pod.
- [x] Implement `grantora-pod-exec` helper for safe health/status/API calls.
- [x] Implement `bootstrap-workspace` action.
- [x] Create or reuse a default workspace for the module instance.
- [x] Seed default runtime permissions by calling `GET /v1/admin/permissions` or equivalent upstream behavior.
- [ ] Create default roles:
  - [x] read-only capability user;
  - [x] side-effect capability user only if enabled by admin;
  - [x] operator/admin role only for Admin API credentials when supported.
- [x] List built-in capability templates through `GET /v1/admin/capability-templates`.
- [x] Add helper actions for common provider setup:
  - [x] create application instance;
  - [x] create capability from template;
  - [x] create/rotate secret;
  - [x] create agent;
  - [x] rotate agent token;
  - [x] create binding.
- [x] Store module-created object ids in `state/bootstrap.json` for idempotency.
- [x] Ensure one-time agent tokens are returned only in the action response and optionally stored only when the admin explicitly requests it.

Acceptance:

- [ ] A CLI or UI operator can create a workspace, app instance, capability, user, role, binding, secret and agent without raw SQL or manual API exploration.
- [ ] Re-running bootstrap does not duplicate objects.
- [ ] Agent token plaintext handling is explicit and safe.
- [ ] All changes are visible through Grantora Admin API and audit records.
- [ ] No bootstrap action requires a host-mapped Grantora Admin API port.

Upstream Grantora work:

- [ ] Add conflict-safe idempotent admin APIs or an `apply desired state` endpoint if module-side idempotency becomes too complex.
- [ ] Add stable machine-readable capability template metadata for UI rendering, including required secret types and human descriptions.

## Milestone 5 — Admin UI MVP

Objective: provide a minimal NS8 UI for installing and operating Grantora safely.

Views:

- [x] Configuration:
  - [x] public hostname;
  - [x] selected user provider;
  - [x] image tag/version;
  - [x] metrics flag;
  - [x] retention windows;
  - [x] rate limits;
  - [x] upstream TLS/timeout defaults.
- [x] Status:
  - [x] pod status;
  - [x] individual helper container statuses;
  - [x] health/readiness;
  - [x] APISIX sync status;
  - [x] public runtime URL;
  - [x] last user sync;
  - [x] running version.
- [x] Workspace bootstrap:
  - [x] create/reuse workspace;
  - [x] sync users;
  - [x] list templates.
- [x] Agents:
  - [x] list agents;
  - [x] create agent;
  - [x] rotate token;
  - [x] disable agent.
- [x] Applications/capabilities:
  - [x] create application instance;
  - [x] create capability from template;
  - [x] create binding;
  - [x] create/rotate secret.
- [x] Audit/usage basic viewer:
  - [x] last N audit events;
  - [x] usage summary by agent/user/capability.

Rules:

- [x] UI calls NS8 actions only.
- [x] UI never stores or logs tokens except showing one-time generated tokens returned by an explicit action.
- [x] UI validates host and domain before calling configure.
- [x] UI clearly distinguishes public runtime URL from private operator surfaces.

Acceptance:

- [ ] A cluster admin can configure the module from UI without CLI.
- [ ] A cluster admin can select one installed user provider from UI.
- [ ] A cluster admin can verify pod/runtime health and APISIX sync from UI.
- [ ] A cluster admin can create a demo/read-only capability flow from UI.

Upstream Grantora work:

- [ ] Add any missing list/status fields required by UI instead of scraping logs.
- [ ] Keep Admin API response shapes stable and documented in upstream `CONTRACTS.md`.

## Milestone 6 — Backup, restore and disaster recovery

Objective: implement reliable NS8 backup/restore for Grantora state.

Tasks:

- [x] Add `state-include.conf` for:
  - [x] generated env files;
  - [x] `secrets.env`;
  - [x] PostgreSQL dumps or volume data as appropriate;
  - [x] `bootstrap.json`;
  - [x] user-domain sync state.
- [x] Add `state-exclude.conf` for:
  - [x] transient logs;
  - [x] temporary dumps;
  - [x] APISIX generated/cache state if it is recreated by reconciliation.
- [x] Implement `backup-module`:
  - [x] stop or quiesce writes if required;
  - [x] run `pg_dump --format=custom` through `podman exec grantora-postgres`;
  - [x] include environment-managed keys and peppers;
  - [x] avoid logging passwords.
- [x] Implement `restore-module`:
  - [x] restore secrets/env first;
  - [x] start `grantora-pod.service`;
  - [x] start `grantora-postgres.service`;
  - [x] restore custom dump through `podman exec grantora-postgres`;
  - [x] start `grantora-apisix-etcd.service`;
  - [x] start `grantora-api.service`;
  - [x] wait for readiness through `grantora-admin`/pod-local call;
  - [x] start `grantora-apisix.service`;
  - [x] run APISIX sync;
  - [x] run smoke checks.
- [x] Implement `run-smoke` action covering direct pod-local health, readiness, APISIX sync and one runtime discovery call when a test agent exists.

Acceptance:

- [ ] Restoring into a clean module instance preserves workspaces, users, apps, capabilities, agents, bindings, encrypted secrets, audit and usage.
- [ ] Restored secrets decrypt existing Grantora encrypted secrets.
- [ ] APISIX routes are regenerated after restore.
- [ ] Backup and restore do not persist or expose raw runtime bearer tokens beyond explicitly generated agent-token files.
- [ ] Restore does not require any temporary public admin/database port.

Upstream Grantora work:

- [ ] Add versioned production database migrations before declaring module production-ready.
- [ ] Add backup/restore compatibility tests across at least one version upgrade once migrations exist.
- [ ] Keep upstream backup/restore smoke compatible with NS8 restore order.

## Milestone 7 — Upgrade and lifecycle management

Objective: support safe module and upstream Grantora upgrades.

Tasks:

- [x] Use immutable Grantora image tags for production releases.
- [x] Add action logic to change `GRANTORA_VERSION` safely.
- [x] Before upgrade:
  - [x] back up PostgreSQL;
  - [x] preserve env secrets;
  - [x] record current running version;
  - [x] record APISIX sync status;
  - [x] record current container image digests.
- [x] During upgrade:
  - [x] pull new images;
  - [x] restart `grantora-api.service` with the new image;
  - [x] run upstream migrations when available;
  - [x] restart helper units only when their image/config changed;
  - [x] wait for `/readyz`;
  - [x] run APISIX reconciliation;
  - [x] run smoke checks.
- [x] On failed upgrade:
  - [x] keep old route state safe;
  - [x] preserve database backup;
  - [x] return actionable error without secret leakage.
- [x] Implement `rotate-module-secrets` for module-level secrets where safe:
  - [x] APISIX Admin key;
  - [x] admin bootstrap token;
  - [x] agent token pepper only with explicit migration/rotation support;
  - [x] encryption key only after upstream supports re-encryption.

Acceptance:

- [ ] Upgrade from one tested module version to the next preserves runtime invocation behavior.
- [ ] Failed APISIX sync does not broaden route exposure.
- [ ] Helper container restart is isolated where possible.
- [ ] UI reports exact upstream and module version.

Upstream Grantora work:

- [ ] Add migration command and documented upgrade policy.
- [ ] Add encryption-key rotation/re-encryption command before module exposes key rotation.
- [ ] Add token-pepper rotation strategy before module exposes pepper rotation.

## Milestone 8 — Security hardening

Objective: verify that NS8 packaging preserves Grantora security boundaries.

Tasks:

- [x] Verify no public route exposes:
  - [x] `/v1/admin/*`;
  - [x] `/healthz` unless intentionally allowed by future private route policy;
  - [x] `/readyz` unless intentionally allowed by future private route policy;
  - [x] `/metrics` unless exposed to an operator-only collector;
  - [x] Grantora API direct port;
  - [x] APISIX Admin API;
  - [x] PostgreSQL;
  - [x] etcd.
- [x] Ensure the pod publishes only `127.0.0.1:${TCP_PORT}:9080` by default.
- [x] Ensure generated files containing secrets are mode `0600`.
- [x] Scrub action errors before returning them to UI.
- [x] Disable shell tracing around secret operations.
- [x] Add log redaction tests for action scripts.
- [x] Add firewall/route/pod-port checks in Robot tests.
- [x] Add SSRF-sensitive base URL expectations to UI and docs: upstream application URLs are entered into Grantora and validated by upstream.
- [x] Keep OIDC/trusted-proxy admin identity disabled unless a future NS8-private proxy path is implemented.

Acceptance:

- [ ] External scan reaches only the configured APISIX runtime route.
- [ ] Agents can only access filtered runtime endpoints with bearer token authentication.
- [ ] Admin/API credentials do not appear in systemd status, logs, UI responses or test artifacts.
- [ ] Denied invocations still produce Grantora audit/usage records.
- [ ] `podman pod port grantora` shows only the runtime port mapping by default.

Upstream Grantora work:

- [ ] Keep optional trusted-proxy admin identity fail-closed and disabled by default.
- [ ] Add a deployment self-check endpoint or admin status field if route exposure validation needs upstream support.

## Milestone 9 — Observability and operations

Objective: make the module operable by NS8 administrators.

Tasks:

- [x] Expose service logs through standard NS8 log collection.
- [x] Keep Grantora logs structured JSON in production.
- [x] Add `get-status` fields for:
  - [x] pod status;
  - [x] PostgreSQL container status;
  - [x] etcd container status;
  - [x] Grantora API container status;
  - [x] APISIX container status;
  - [x] API liveness;
  - [x] API readiness;
  - [x] APISIX sync status;
  - [x] last sync error;
  - [x] selected domain;
  - [x] user sync status;
  - [x] container versions.
- [x] Add optional metrics integration:
  - [x] keep `/metrics` private;
  - [x] access metrics only through pod-local helper unless a future operator-only collector integration is added;
  - [x] document how NS8/prometheus should scrape it if enabled.
- [x] Add retention action/timer for audit and usage if upstream CLI/API is available from the image.
- [x] Add troubleshooting commands in README:
  - [x] invalid admin bootstrap hash;
  - [x] missing or wrong encryption key;
  - [x] APISIX sync failure;
  - [x] PostgreSQL readiness failure;
  - [x] user-domain sync failure;
  - [x] pod/container unit failure.

Acceptance:

- [ ] Admin UI and CLI status are enough to diagnose common failures without manual container shell access.
- [ ] Metrics/logs do not include secrets or sensitive payloads.
- [ ] Retention can be dry-run and then applied safely.

Upstream Grantora work:

- [ ] Expose or keep stable the retention command/API used by the module.
- [ ] Keep metric names stable or document changes before module release updates.

## Milestone 10 — Test matrix and CI

Objective: prove lifecycle, pod topology, security and restore behavior before release.

Tests:

- [ ] Action unit tests:
  - [ ] JSON schema validation;
  - [ ] env rendering;
  - [ ] secret generation idempotency;
  - [ ] admin hash generation;
  - [ ] domain discovery parsing;
  - [ ] safe error formatting.
- [ ] Systemd/pod tests:
  - [ ] `grantora-pod.service` creates one pod;
  - [ ] helper services join the same pod;
  - [ ] only APISIX runtime port is published;
  - [ ] container units restart independently where safe.
- [ ] Robot lifecycle tests:
  - [ ] install module;
  - [ ] configure with selected user domain;
  - [ ] verify Traefik runtime route;
  - [ ] verify direct Grantora API route is not public;
  - [ ] verify APISIX Admin API is not public;
  - [ ] verify PostgreSQL and etcd are not public;
  - [ ] run user sync;
  - [ ] create demo capability flow;
  - [ ] invoke runtime endpoint through APISIX;
  - [ ] backup;
  - [ ] restore;
  - [ ] invoke again;
  - [ ] destroy module.
- [ ] Upgrade tests:
  - [ ] start previous released module;
  - [ ] seed state;
  - [ ] upgrade;
  - [ ] verify state and route.
- [ ] Security tests:
  - [ ] no secret in action output;
  - [ ] no secret in logs;
  - [ ] no public admin endpoints;
  - [ ] no direct PostgreSQL/APISIX Admin/etcd exposure;
  - [ ] no default host admin port;
  - [ ] disabled agent denied;
  - [ ] no binding denied;
  - [ ] agent cannot act for another user.
- [ ] CI workflows:
  - [ ] markdown/lint;
  - [ ] shellcheck or equivalent;
  - [ ] action unit tests;
  - [ ] build module image;
  - [ ] Robot tests using NS8 test infrastructure;
  - [ ] optional manual full e2e/backup-restore gate.

Acceptance:

- [ ] CI blocks releases unless lifecycle and security tests pass.
- [ ] Manual full e2e validates install -> configure -> sync -> invoke -> backup -> restore -> invoke.
- [ ] Test artifacts do not contain secrets.

Upstream Grantora work:

- [ ] Keep upstream unit/integration/e2e/security gates green for every image consumed by the module.
- [ ] Publish release evidence or artifacts that the module can reference during release.

## Milestone 11 — Documentation and operator experience

Objective: document installation, pod topology, configuration, operation, security and troubleshooting.

Tasks:

- [ ] Rewrite `README.md` for NS8 operators.
- [ ] Document install from image and from Software Center when repository metadata exists.
- [ ] Document the pod/container systemd topology.
- [ ] Document configuration fields.
- [ ] Document selected user provider behavior.
- [ ] Document public/private exposure matrix.
- [ ] Document backup/restore order.
- [ ] Document upgrade procedure.
- [ ] Document first provider setup examples:
  - [ ] mock/demo capability;
  - [ ] NethVoice phonebook search;
  - [ ] Nextcloud files search.
- [ ] Document how to generate and store upstream provider secrets safely.
- [ ] Document agent integration with `/v1/mcp/tools`, `/v1/mcp/call`, filtered OpenAPI and runtime bearer tokens.
- [ ] Document unsupported features and future work.

Acceptance:

- [ ] A new NS8 admin can install and configure Grantora from README and UI.
- [ ] Documentation clearly says Grantora runtime endpoint is public to agents, while admin/operator surfaces are private and pod-local.
- [ ] Documentation does not instruct users to paste upstream secrets into agents.

Upstream Grantora work:

- [ ] Keep upstream `docs/ns8-packaging.md` updated whenever module assumptions change.
- [ ] Keep upstream `OPERATIONS.md`, `SECURITY.md` and `CONTRACTS.md` aligned with consumed image versions.

## Milestone 12 — Production readiness and release

Objective: publish a production-capable NS8 module.

Prerequisites:

- [ ] Upstream Grantora publishes immutable release images.
- [ ] Upstream Grantora has versioned production database migrations.
- [ ] Module backup/restore is tested on clean NS8 instances.
- [ ] Module upgrade is tested from at least one previous module version.
- [ ] Security exposure tests pass.
- [ ] Pod topology tests pass.
- [ ] Admin UI supports provider selection and core operations.
- [ ] Documentation is complete.

Release tasks:

- [ ] Tag module release.
- [ ] Publish module image.
- [ ] Generate/update repository metadata for the target NS8 repository.
- [ ] Run install from published image.
- [ ] Run install from repository metadata/Software Center when available.
- [ ] Run full acceptance test matrix.
- [ ] Record tested upstream Grantora image tag and digest.

Production acceptance:

- [ ] Fresh install succeeds.
- [ ] Configure succeeds with selected NS8 user provider.
- [ ] One pod and four runtime containers start correctly.
- [ ] Public runtime route works.
- [ ] Admin API is private and pod-local.
- [ ] User sync works.
- [ ] A real or mock capability can be invoked by an authenticated agent.
- [ ] Audit and usage records are produced.
- [ ] Backup and restore preserve working invocation.
- [ ] Upgrade preserves working invocation.
- [ ] No known high/critical container or dependency vulnerability is accepted without documented exception.

## Upstream Grantora feature backlog required by ns8-grantora

Track these in `Stell0/grantora` before marking `ns8-grantora` production-ready.

### UP-1 — Versioned production database migrations

Reason: current pre-release schema bootstrap is acceptable for disposable development, not production upgrades.

Required upstream work:

- [ ] Add migration framework or equivalent versioned schema upgrader.
- [ ] Add CLI command or startup mode for migration execution.
- [ ] Document backup-before-upgrade and rollback boundaries.
- [ ] Add migration tests from previous released schema to current schema.
- [ ] Update `PROJECT.md`, `CONTRACTS.md`, `OPERATIONS.md` and `TESTING.md`.

### UP-2 — Idempotent/bulk user sync API

Reason: NS8 selected account domain can contain many users; module-side per-user create/list/patch loops are fragile and cannot update all metadata cleanly.

Required upstream work:

- [ ] Add `POST /v1/admin/users/sync` or equivalent idempotent API.
- [ ] Support source ownership, e.g. `source="ns8:<domain>"`.
- [ ] Support create/update/disable in one transaction per workspace.
- [ ] Return counts and safe errors only.
- [ ] Audit the sync summary.
- [ ] Add tests for workspace isolation and disabled user behavior.

### UP-3 — Mutable user metadata and optional group metadata

Reason: LDAP display names, mail addresses and future group-based binding helpers need metadata updates after initial creation.

Required upstream work:

- [ ] Allow safe update of `display_name` and optional `email`.
- [ ] Decide whether groups are stored as Grantora metadata or represented only by module-side binding helpers.
- [ ] Preserve runtime authorization deny-by-default.
- [ ] Add tests for metadata update without cross-workspace leakage.

### UP-4 — Stable release image contract

Reason: NS8 module releases must pin known-good upstream image tags and preferably digests.

Required upstream work:

- [ ] Publish release images under a stable registry namespace.
- [ ] Document supported tags and digest pinning policy.
- [ ] Keep `/healthz` version reporting.
- [ ] Keep release smoke test mandatory before publishing.

### UP-5 — Secret/key rotation support

Reason: module can generate initial secrets, but safe production rotation requires application support.

Required upstream work:

- [ ] Add encryption-key re-encryption workflow before exposing key rotation in NS8.
- [ ] Add token-pepper rotation workflow or document that pepper rotation invalidates all tokens.
- [ ] Add tests proving old encrypted secrets remain readable after re-encryption.

### UP-6 — Operator status API, only if module status needs more than current APIs

Reason: module UI should not parse logs to report operational status.

Current APIs already cover health, readiness, APISIX status, audit, usage and core object lists. Add more only if needed.

Potential upstream work:

- [ ] Add a concise `GET /v1/admin/status` returning safe aggregate status.
- [ ] Include dependency status, APISIX sync status, counts by object type and current version.
- [ ] Exclude secrets, tokens, raw upstream payloads and internal URLs.

### UP-7 — Pod-friendly packaging notes

Reason: upstream compose uses service names and multiple networks; NS8 module uses one Podman pod with pod-local `127.0.0.1` service URLs and one published APISIX runtime port.

Required upstream work:

- [ ] Document pod-local equivalents for `DATABASE_URL`, `APISIX_ADMIN_URL`, APISIX etcd endpoint and `APISIX_RUNTIME_UPSTREAM_NODE`.
- [ ] Keep APISIX config templating compatible with pod-local etcd and admin endpoints.
- [ ] Keep `docs/ns8-packaging.md` clear that Compose network topology is an example, not a required runtime dependency.

## First implementation slice

Start with the smallest useful vertical slice:

1. Initialize repository naming and docs.
2. Create `grantora-pod.service` with one loopback APISIX runtime port.
3. Add separate systemd units for PostgreSQL, APISIX etcd, Grantora API and APISIX in the pod.
4. Generate secrets/env files.
5. Configure public Traefik route to APISIX runtime only.
6. Start `grantora.service` and sync APISIX through pod-local Admin API access.
7. Expose `get-status` with pod and per-container status.
8. Add UI provider selector backed by domain discovery.
9. Bind selected domain and run one-shot user sync.
10. Bootstrap a mock capability and invoke it through the public runtime route.
11. Add backup/restore smoke.

Do not start broad adapter UI work until this slice is green.